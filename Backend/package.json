{
  "private": true,
  "scripts": {
    "test:rules": "firebase emulators:exec --only firestore \"npx jest test/firestore-rules.test.ts --verbose\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^3.0.0",
    "@types/jest": "^30.0.0",
    "firebase": "^10.0.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.2.0",
    "typescript": "^5.8.0"
  },
  "jest": {
    "preset": "ts-jest",
    "testEnvironment": "node",
    "testTimeout": 30000
  }
}
